{
  "name": "automatic-initiative-2",
  "module": "index.ts",
  "release": {
    "branches": ["master", "next"]
  },
  "scripts": {
    "build": "vite build",
    "deploy": "bun zip-and-deploy.js",
    "release": "bun run build && bun run deploy"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "rollup-plugin-copy": "^3.5.0",
    "vite": "^6.0.6"
  },
  "type": "module",
  "dependencies": {
    "archiver": "^7.0.1",
    "semantic-release": "^24.2.0"
  }
}